import {BookOpen,Compass,GitCompareArrows,History,Map,Route,Users} from 'lucide-react';import type {ViewId} from '../../types/philosophy';
const items:[ViewId,string,typeof History][]=[['history','Big History',History],['branches','Branch Explorer',Compass],['map','Philosophy Map',Map],['philosophers','Philosophers',Users],['compare','Compare',GitCompareArrows],['paths','Learning Paths',Route]];
export function Navigation({view,onChange}:{view:ViewId;onChange:(v:ViewId)=>void}){return <nav aria-label="Primary navigation"><button className="brand" onClick={()=>onChange('branches')}><BookOpen size={20}/><span>Philosophy <b>Atlas</b></span></button><div className="nav-scroll">{items.map(([id,label,Icon])=><button key={id} className={view===id?'active':''} onClick={()=>onChange(id)}><Icon size={16}/><span>{label}</span></button>)}</div></nav>}
