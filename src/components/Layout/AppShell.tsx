import type {ReactNode} from 'react';import type {ViewId} from '../../types/philosophy';import {Navigation} from './Navigation';import {GlobalSearch} from '../Search/GlobalSearch';
export function AppShell({view,onView,openBranch,openPhilosopher,children}:{view:ViewId;onView:(v:ViewId)=>void;openBranch:(id:string)=>void;openPhilosopher:(id:string)=>void;children:ReactNode}){return <div className="shell"><header><Navigation view={view} onChange={onView}/><GlobalSearch openBranch={openBranch} openPhilosopher={openPhilosopher}/></header><main>{children}</main><footer><span>Philosophy Atlas · A local-first museum of questions</span><span>Explore arguments, not slogans.</span></footer></div>}
